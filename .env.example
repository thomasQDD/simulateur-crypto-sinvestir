# --- Sources de données (les deux clés sont OPTIONNELLES) ---

# CoinGecko : catalogue & recherche de cryptos + logos (plan Demo gratuit).
# Sans clé, l'API publique fonctionne avec des limites plus basses.
COINGECKO_API_KEY=

# CryptoCompare : prix historiques journaliers (plusieurs années, gratuit).
# Sans clé, l'API publique fonctionne ; une clé augmente le quota.
CRYPTOCOMPARE_API_KEY=
